<!DOCTYPE html>
<html dir="ltr">

<head>
  <%- include (includes("head")); %>
</head>

<body id="target" class="pt-5 pb-5">
  <%- include (includes("header")) %>
  <div class="container pt-5 ml-auto mr-auto">
    <div class="row pt-3">
      <form class="ml-auto mr-auto" name="file_submit_form" id="file_submit_form" role="form" method="POST"
        enctype="multipart/form-data" action="/user_create">
        <div class="form-group row">
          <label for="file" class="col-sm-4 col-form-label">엑셀 파일</label>
          <input type="file" class="col-sm-auto form-control" name="file" id="file" value="파일" />
        </div>
        <div class="form-group text-center col-xs-auto">
          <input type="submit" class="btn btn-primary" name="submitButton" value="생성" />
        </div>
      </form>
    </div>
  </div>
  <%- include (includes("modal")) %>
  <%- include (includes("footer")) %>
</body>
<%- include (includes("foot")); %>

</html>